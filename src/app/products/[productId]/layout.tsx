export default function ProductLayout({
    children,
  }: {
    children: React.ReactNode
  }) {
    return (
        <div>
          <h2>Product layout</h2>
          {children}
  
        </div>
    )
  }